# Contracts

On-chain work belongs under [`../soroban/`](../soroban/README.md) as **Soroban** (Rust) contracts on the **Stellar** network.

This folder is reserved for future Soroban project layout (e.g. a Rust workspace) or can be removed once you add a `soroban/contracts` crate.
